<form method="POST" action="">
  <fieldset>
    <legend>Add New Expense</legend>
    <!-- TITLE -->
    <div class="form-group row">
  <label for="example-datetime-local-input" class="col-2 col-form-label">Expense Type</label>
  <div class="col-10">
      <select name="expenseType" id="expType">
          {{#each expenseType}}
  <option value={{this}}>{{this}}</option>
 {{/each}}
</select>
       
    </p>
    <div class="form-group row">
  <label for="example-datetime-local-input" class="col-2 col-form-label">Date and time</label>
  <div class="col-10">
    <input type="text" class="form-control" id="datepicker" data-date-end-date="0d">
  </div>
<div class="custom-file">
    <input type="file" class="custom-file-input" id="validatedCustomFile" required>
    <label class="custom-file-label" for="validatedCustomFile">Choose file...</label>
    <div class="invalid-feedback">Example invalid custom file feedback</div>
  </div>


  <!-- BUTTON -->
 <input class="btn btn-primary" type="submit" value="Submit">
<input class="btn btn-primary" type="reset" value="Reset">
</form>